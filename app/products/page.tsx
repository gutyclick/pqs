import type {Metadata} from "next";import Image from "next/image";import Link from "next/link";import {ArrowRight} from "lucide-react";import {PageHero} from "@/components/layout/PageHero";import {products} from "@/data/site";
export const metadata:Metadata={title:"Products",description:"Explore squash, Quetzali watermelon and pineapple produced and packed in Panama.",alternates:{canonical:"/products"}};
export default function ProductsPage(){return <><PageHero eyebrow="Product portfolio" title="Selected at origin. Ready for markets." description="Discover agricultural products grown and handled in Panama, with formats prepared around buyer requirements."/><section className="section"><div className="container space-y-6">{products.map((p)=><Link href={`/products/${p.slug}`} key={p.slug} className="group grid overflow-hidden border border-[var(--line)] lg:grid-cols-[.85fr_1.15fr]"><div className="relative min-h-[360px]"><Image src={p.image} alt={`${p.name} from Panama`} fill sizes="(max-width:1024px) 100vw,45vw" className="object-cover transition duration-700 group-hover:scale-[1.025]"/></div><div className="flex flex-col justify-center p-8 md:p-14"><p className="eyebrow text-[#768078]">{p.scientific}</p><h2 className="display mt-5 text-5xl md:text-7xl">{p.name}</h2><p className="mt-6 max-w-xl leading-7 text-[#59645d]">{p.description}</p><span className="mt-9 inline-flex items-center gap-2 font-semibold">Product details <ArrowRight size={17}/></span></div></Link>)}</div></section></>}
