import type{Metadata}from"next";import Image from"next/image";import Link from"next/link";import{PageHero}from"@/components/layout/PageHero";import{posts}from"@/data/site";export const metadata:Metadata={title:"Noticias",description:"Noticias de Panama Squash Co. sobre agricultura, productos, cosechas y exportación.",alternates:{canonical:"/blog"}};export default function Blog(){return <><PageHero eyebrow="Noticias y conocimiento" title="Actualidad desde el campo." description="Información sobre nuestros productos, producción y el comercio agrícola que conecta a Panamá con otros mercados."/><section className="section"><div className="container grid gap-x-6 gap-y-14 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><article key={p.slug}><Link href={`/blog/${p.slug}`} className="group"><div className="relative aspect-[4/3] overflow-hidden rounded-[24px]"><Image src={p.image} alt="" fill sizes="(max-width:768px) 100vw,33vw" className="object-cover transition duration-500 group-hover:scale-105"/></div><p className="eyebrow mt-6 text-[var(--brand-primary)]">{p.category} · {p.date}</p><h2 className="mt-4 text-2xl font-semibold leading-tight">{p.title}</h2><p className="mt-3 leading-7 text-[var(--muted)]">{p.excerpt}</p></Link></article>)}</div></section></>}
