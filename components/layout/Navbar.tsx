"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useEffect, useState } from "react";
const links=[['Home','/'],['About','/about'],['Products','/products'],['Export','/#export'],['Gallery','/gallery'],['Blog','/blog'],['Contact','/contact']];
export function Navbar(){const[open,setOpen]=useState(false);const[solid,setSolid]=useState(false);useEffect(()=>{const fn=()=>setSolid(scrollY>30);fn();addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${solid||open?'bg-[#14221d]/95 py-0 shadow-sm backdrop-blur-md':'bg-transparent py-2'}`}><nav className="container flex h-20 items-center justify-between text-white" aria-label="Primary navigation"><Link href="/" className="leading-none" aria-label="Panama Squash Co. home"><span className="block font-[family-name:var(--font-display)] text-xl tracking-[-.04em]">PANAMA SQUASH</span><span className="eyebrow mt-1 block text-[8px] text-[#d5a553]">Agriculture · Export</span></Link><div className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><Link key={href+label} href={href} className="text-[13px] font-medium transition hover:text-[#efb457]">{label}</Link>)}<Link href="/contact" className="button button-primary !py-3">Request information</Link></div><button onClick={()=>setOpen(!open)} className="p-2 lg:hidden" aria-expanded={open} aria-label={open?'Close menu':'Open menu'}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className="container border-t border-white/15 pb-6 lg:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href+label} href={href} className="block border-b border-white/10 py-4 text-lg text-white">{label}</Link>)}</div>}</header>}
