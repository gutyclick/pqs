import type {Metadata} from "next";import {notFound} from "next/navigation";import Image from "next/image";import Link from "next/link";import {ArrowRight,Check} from "lucide-react";import {products} from "@/data/site";
export function generateStaticParams(){return products.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return{};return{title:p.name,description:p.description,alternates:{canonical:`/products/${slug}`}}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <><section className="grid min-h-[760px] bg-[var(--brand-dark)] pt-20 text-white lg:grid-cols-2"><div className="container flex items-end py-20 lg:w-auto lg:max-w-[590px] lg:ml-auto lg:mr-0 lg:px-10"><div><p className="eyebrow text-[#e9bd72]">{p.scientific}</p><h1 className="display mt-6 text-6xl md:text-8xl">{p.name}</h1><p className="mt-7 max-w-lg text-lg leading-8 text-white/65">{p.description}</p></div></div><div className="relative min-h-[500px]"><Image src={p.image} alt={`${p.name} produced by Panama Squash Co.`} fill priority sizes="(max-width:1024px) 100vw,50vw" className="object-cover"/></div></section><section className="section"><div className="container grid gap-12 lg:grid-cols-2"><h2 className="display text-5xl md:text-7xl">Product<br/>specifications.</h2><div>{p.features.map(f=><div key={f} className="flex gap-4 border-b border-[var(--line)] py-5"><Check className="mt-1 text-[var(--brand-primary)]" size={18}/><span>{f}</span></div>)}<p className="mt-8 text-sm leading-6 text-[#6a746e]">Availability, volumes and commercial terms are confirmed for each inquiry.</p><Link href={`/contact?product=${p.slug}`} className="button button-primary mt-8">Request information <ArrowRight size={16}/></Link></div></div></section></>}
