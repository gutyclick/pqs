import Image from"next/image";import Link from"next/link";import{ArrowRight}from"lucide-react";import{PageHero}from"@/components/layout/PageHero";import{additionalProducts,products}from"@/data/site";import{createPageMetadata}from"@/lib/seo";
export const metadata=createPageMetadata({title:"Agricultural products from Panama",description:"Explore squash, watermelon, pineapple, plantain, papaya, waxed cassava and Golden Honeydew melon grown and selected in Panama.",path:"/products",image:"/calabaza/portada_calabaza.png"});
export default function ProductsPage(){return <><PageHero eyebrow="Agricultural portfolio" title="Selected at origin. Prepared for your market." description="Products grown and handled in Panama, available in formats designed around each buyer's needs."/><section className="section"><div className="container space-y-6">{products.map(p=><Link href={`/products/${p.slug}`} key={p.slug} className="group grid overflow-hidden rounded-[32px] bg-[var(--brand-light)] lg:grid-cols-[.85fr_1.15fr]"><div className="relative min-h-[380px]"><Image src={p.image} alt={`${p.name} de Panama`} fill sizes="(max-width:1024px) 100vw,45vw" className="object-cover transition duration-700 group-hover:scale-[1.025]"/></div><div className="flex flex-col justify-center p-8 md:p-14"><p className="eyebrow text-[var(--brand-primary)]">{p.scientific}</p><h2 className="display mt-5 text-5xl md:text-7xl">{p.name}</h2><p className="mt-6 max-w-xl leading-7 text-[var(--muted)]">{p.description}</p><span className="button button-dark mt-9 w-fit">View product <ArrowRight size={17}/></span></div></Link>)}</div><div className="container mt-24"><p className="eyebrow text-[var(--brand-primary)]">More from our land</p><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{additionalProducts.map(p=><Link href={`/products/otros#${p.slug}`} key={p.slug} className="group"><div className="relative aspect-[4/5] overflow-hidden rounded-[26px] bg-[var(--brand-light)]"><Image src={p.cover} alt={`${p.name} de Panama`} fill sizes="(max-width:640px) 100vw,(max-width:1024px) 50vw,25vw" className="object-cover transition duration-700 group-hover:scale-105"/><span className="absolute right-4 top-4 grid h-11 w-11 place-items-center rounded-full bg-white text-[var(--brand-dark)]"><ArrowRight size={17}/></span><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-[#082d22]/85 to-transparent p-6 pt-20 text-white"><p className="eyebrow text-[var(--brand-accent)]">{p.scientific}</p><h2 className="display mt-3 text-3xl">{p.name}</h2></div></div></Link>)}</div></div></section></>}
