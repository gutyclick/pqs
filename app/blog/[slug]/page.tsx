import type {Metadata} from "next";import Image from "next/image";import {notFound} from "next/navigation";import {posts} from "@/data/site";
export function generateStaticParams(){return posts.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=posts.find(x=>x.slug===slug);return p?{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${slug}`}}:{}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <article><header className="bg-[var(--brand-dark)] pb-16 pt-40 text-white"><div className="container max-w-4xl"><p className="eyebrow text-[#e9bd72]">{p.category} · {p.date}</p><h1 className="display mt-7 text-5xl md:text-8xl">{p.title}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-white/65">{p.excerpt}</p></div></header><div className="container max-w-5xl py-12"><div className="relative aspect-[16/8]"><Image src={p.image} alt="Panama Squash Co. agricultural operations" fill priority sizes="100vw" className="object-cover"/></div><div className="prose mx-auto py-14"><p>This initial article is a content placeholder prepared for migration to Supabase. Final editorial copy will be added before publication.</p><h2>Quality begins at origin</h2><p>Product handling starts in the field and continues through selection and packing. Each stage contributes to the consistency buyers expect.</p><p>Contact our team for current product availability, formats and commercial information.</p></div></div></article>}
