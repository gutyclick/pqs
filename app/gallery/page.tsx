import type{Metadata}from"next";import{PageHero}from"@/components/layout/PageHero";import{GalleryGrid}from"@/components/gallery/GalleryGrid";import{getGalleryCollections}from"@/lib/content";export const metadata:Metadata={title:"Galería",description:"Conoce nuestros campos, productos, cosechas y operación agrícola en Panamá.",alternates:{canonical:"/gallery"}};export const revalidate=60;export default async function GalleryPage(){const collections=await getGalleryCollections();return <><PageHero eyebrow="Nuestro origen" title="La agricultura, de cerca." description="Una mirada a nuestros productos, campos y a los procesos detrás de cada cosecha."/><section className="section"><div className="container space-y-24">{collections.map(collection=><section key={collection.id}><div className="mb-10 grid gap-4 border-b border-[var(--line)] pb-7 md:grid-cols-[1fr_.8fr] md:items-end"><h2 className="display text-4xl md:text-6xl">{collection.title}</h2>{collection.description&&<p className="leading-7 text-[var(--muted)]">{collection.description}</p>}</div><GalleryGrid images={collection.images}/></section>)}</div></section></>}
