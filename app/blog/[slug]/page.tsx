import type{Metadata}from"next";import Image from"next/image";import{notFound}from"next/navigation";import{posts}from"@/data/site";export function generateStaticParams(){return posts.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=posts.find(x=>x.slug===slug);return p?{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${slug}`}}:{}}export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <article><header className="bg-[var(--brand-dark)] pb-20 pt-40 text-white"><div className="container max-w-4xl"><p className="eyebrow text-[var(--brand-accent)]">{p.category} · {p.date}</p><h1 className="display mt-7 text-5xl md:text-8xl">{p.title}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-white/65">{p.excerpt}</p></div></header><div className="container max-w-5xl py-12"><div className="relative aspect-[16/8] overflow-hidden rounded-[32px]"><Image src={p.image} alt="Operación agrícola de Panama Squash Co." fill priority sizes="100vw" className="object-cover"/></div><div className="prose mx-auto py-14"><p>Este artículo inicial está preparado para migrarse a Supabase. El contenido editorial definitivo se añadirá antes de su publicación.</p><h2>La calidad comienza en el origen</h2><p>El manejo del producto comienza en el campo y continúa durante la selección y el empaque. Cada etapa contribuye a la consistencia que espera el comprador.</p><p>Contacta a nuestro equipo para conocer la disponibilidad, los formatos y la información comercial vigente.</p></div></div></article>}
