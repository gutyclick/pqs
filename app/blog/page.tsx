import type {Metadata} from "next";import Image from "next/image";import Link from "next/link";import {PageHero} from "@/components/layout/PageHero";import {posts} from "@/data/site";
export const metadata:Metadata={title:"News & Insights",description:"News and insights from Panama Squash Co. about agriculture, harvests, products and export.",alternates:{canonical:"/blog"}};
export default function Blog(){return <><PageHero eyebrow="News & insights" title="From the field." description="Updates on our products, production and the agricultural trade connecting Panama to the world."/><section className="section"><div className="container grid gap-x-8 gap-y-16 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><article key={p.slug}><Link href={`/blog/${p.slug}`}><div className="relative aspect-[4/3] overflow-hidden"><Image src={p.image} alt="" fill sizes="(max-width:768px) 100vw,33vw" className="object-cover transition duration-500 hover:scale-105"/></div><p className="eyebrow mt-6 text-[#777]">{p.category} · {p.date}</p><h2 className="mt-4 text-2xl font-semibold leading-tight">{p.title}</h2><p className="mt-3 leading-7 text-[#657069]">{p.excerpt}</p></Link></article>)}</div></section></>}
